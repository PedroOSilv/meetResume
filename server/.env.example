# Configurações do Servidor Audio AI Backend
# Copie este arquivo para .env e configure suas chaves

# Chave da API OpenAI (obrigatória)
# Obtenha em: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Configurações de Rede
# HOST: IP para aceitar conexões (padrão: 0.0.0.0 - aceita de qualquer IP)
# Para aceitar apenas conexões locais, use: HOST=127.0.0.1
HOST=0.0.0.0

# Porta do servidor (opcional, padrão: 3000)
PORT=3000

# Prompt personalizado para ChatGPT (opcional)
# OPÇÃO 1: Definir prompt diretamente na variável de ambiente
# CHATGPT_PROMPT="Seu prompt personalizado aqui..."

# OPÇÃO 2: Usar arquivo prompt.md (recomendado)
# O servidor automaticamente carregará o prompt do arquivo prompt.md
# Se não existir, usará o prompt padrão

# Ambiente (opcional, padrão: production)
NODE_ENV=development